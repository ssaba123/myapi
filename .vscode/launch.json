{
    "configurations": [
    
        {
            "type": "node",
            "name": "Run Script: start debug",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}